<Window x:Class="pipe.AboutProcess"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="О процессе очистки"
        Height="360"
        Width="600" ResizeMode="NoResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.6*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Image x:Name="imgProcess"
               Source="resources\process.bmp"
               Margin="0,0,0,0"
               Grid.Row="0"
               Grid.Column="0"
               Height="330"
               VerticalAlignment="Top" />
        <RichTextBox x:Name="rtb_info"
                     IsReadOnly="True"
                     Grid.Row="0"
                     Grid.Column="1">
            <FlowDocument>
                <Paragraph>
                    <Run Text="RichTextBox" />
                </Paragraph>
            </FlowDocument>
        </RichTextBox>

    </Grid>
</Window>
